// you are aligible or not for vote

let name = prompt("Enter Your name")
let age = prompt("Enter your age")
if (age >= 18) {
    console.log(`${name} You are aligible for vote`)

} else {
    console.log(`${name} You are not aligible for vote`)
}